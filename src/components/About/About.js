import React, {Fragment} from 'react';
import Header from "../Header/Header";

const About = props => {
    return (
        <Fragment>
            <Header/>
            <h1>About page</h1>
        </Fragment>
    );
};

export default About;